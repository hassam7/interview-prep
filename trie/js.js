const name = '2212';
const age = '3323'
const content = tag
`My dog's name is ${name} and he is ${age} years old`;

function tag(string, ...values) {
  console.log(string);
  console.log(values);
  return 'zxczxc';
}

console.log(content);